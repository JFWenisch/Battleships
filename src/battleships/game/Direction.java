package battleships.game;

/*
 * Direction:
 * We only use EAST and SOUTH, so Ship-coordinates are always describing the most WESTERN/NORTHERN coordinate, they reside in 
 */
public enum Direction {
	EAST, SOUTH
}
